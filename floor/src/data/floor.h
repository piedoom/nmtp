/// data struct representing the floor
struct Floor {
    /// holds on/off position of squares (from 0 - 15)
    bool squares[16] = { 0 };

    /// get specific tiles, etc.
    bool get_square(int x, int y) {
        /// TODO: impl
        return false;
    }
};